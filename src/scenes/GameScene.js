import Phaser from "phaser";



class GameScene extends Phaser.Scene {
    constructor(test) {
        super({
            key: 'GameScene'
        });
    }

    preload() {
        // code here

    }

    create() {
        
    }

    update(delta, time) {
        
    }
}
export default GameScene;
